import { createSlice } from '@reduxjs/toolkit';

const initialState = {
    salesReps: [],
};

export const dashboardSlice = createSlice({
    name: 'dashboard',
    initialState,
    reducers: {},
});
