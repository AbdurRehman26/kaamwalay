export enum ErrorMessages {
    ShouldNotBeEmpty = "%1 shouldn't be empty.",
    ShouldBeValidEmailAddress = '%1 should be a valid email address.',
    ShouldHaveAtLeastNCharacters = '%1 should have at least %2 characters!',
}
